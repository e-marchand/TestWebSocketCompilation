name: Build
on:
  push:
  pull_request:

jobs:
  build:
    name: "Build"
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Build
      uses: 4d/build4d-action@main
      with:
        product-line: "21.x"
        version: "21.0"
        build: "100465"

